{
  "name": "parcel-hmr-test",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "parcel src/index.html",
    "build": "parcel build src/index.html",
    "clean": "rimraf dist && rimraf .parcel-cache",
    "serve": "http-server dist"
  },
  "devDependencies": {
    "http-server": "^13.0.2",
    "parcel": "2.0.0-nightly.859",
    "rimraf": "^3.0.2",
    "typescript": "^4.4.3"
  }
}
